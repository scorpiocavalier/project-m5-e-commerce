import React from "react";

// import firebase from "../firebase/firebase";
// import itemsDB from "../data/items.json";
// import companiesDB from "../data/companies.json";

export default function Firestore() {
  // const database = firebase.database();
  // const companyRef = database.ref("companies");
  // const itemsRef = database.ref("items");

  // const companiesJSON = {};
  // companiesDB.forEach((company) => (companiesJSON[company.id] = company));
  // console.log(companiesJSON);

  // const addItem = () => cartRef.push(newItem);
  // const editItem = () => {
  //   console.log("editItem");
  //   companyRef.set(companiesJSON);
  // };

  // const getItem = () => {
  //   itemsRef.once("value").then((snapshot) => {
  //     const items = snapshot.val() || "Not found";
  //     console.log(items); // returns an object of all items
  //   });
  // };

  // const getItemSnapshot = () => {
  //   itemsRef.on("value", (snapshot) => {
  //     console.log(snapshot.val());
  //   });
  // };

  return (
    <>
      {/* <button onClick={}>Nothing</button>; */}
    </>
  );
}
